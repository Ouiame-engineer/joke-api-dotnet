# JokeApi - Technical test (dotnet)

## Prerequisites
- .NET 8 SDK installed (`dotnet --version`)
- (Optional) Visual Studio 2026 or VS Code

## Run locally
1. Restore & run:
```bash
dotnet restore
dotnet run
